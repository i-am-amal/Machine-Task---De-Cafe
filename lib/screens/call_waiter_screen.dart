import 'package:flutter/material.dart';

class CallWaiterScreen extends StatelessWidget {
  const CallWaiterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Call waiter page '),
      ),
    );
  }
}
